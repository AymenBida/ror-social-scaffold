<li>
  Name: <%= user.name %>
  <% if current_user == user %>
    <span> (you) </span>
  <% end %>
  <span class="profile-link">
    <%= link_to 'See Profile', user_path(user), class: 'profile-link' %>
    <%= show_invite_button(user) %>
  </span>
</li>
